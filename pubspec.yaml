name: barcode_scanner_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  analyzer: ^4.2.0
  cupertino_icons: ^1.0.2
  freezed_annotation: ^2.0.3
  dartz: ^0.10.1
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1
  hive: ^2.2.3
  get_it: ^7.2.0
  injectable: ^1.5.3
  hive_flutter: ^1.1.0
  auto_route: ^4.2.1
  flutter_qr_bar_scanner:
    git:
      url: https://github.com/fylyppo/flutter_qr_bar_scanner.git
  uuid: ^3.0.6
  intl: ^0.17.0
  another_flushbar: ^1.10.29
  json_serializable: ^6.3.1
  json_annotation: ^4.6.0
  hydrated_bloc: ^8.1.0
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.1.11
  freezed: ^2.0.3+1
  injectable_generator: ^1.5.3
  hive_generator: ^1.1.3
  auto_route_generator: ^4.2.0

dependency_overrides:
  analyzer: ^4.2.0

flutter:

  uses-material-design: true
